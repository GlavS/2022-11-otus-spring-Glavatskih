spring:
  datasource:
    url: jdbc:h2:mem:bookdb
  sql:
    init:
      mode: always
      schema-locations: schema.sql
      data-locations: data.sql
  jpa:
    generate-ddl: false
    hibernate:
      ddl-auto: none
    show-sql: true
logging:
  level:
    ROOT: ERROR