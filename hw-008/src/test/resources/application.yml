spring:
  data:
    mongodb:
      database: super
  mongodb:
    embedded:
      version: 3.5.5


  shell:
    interactive:
      enabled: false
    command:
      script:
        enabled: false


logging:
  level:
    root: ERROR